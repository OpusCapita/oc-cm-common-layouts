/* eslint-disable no-confusing-arrow */
import styled from 'styled-components';
import { theme } from '../constants';

const PrimitiveHeaderTitle = styled.h1`
font-size: ${theme.fontSizes.title};
font-weight: 600;
margin: 0;
`;
export default PrimitiveHeaderTitle;
